# Repository guidance

When UI elements or configuration keys are added, changed, or removed, update the matching procedures in `README.md`, `docs/USER_MANUAL.md`, and `docs/SPECIFICATION.md` in the same change.

After changing either Markdown document, regenerate its distribution DOCX and verify the rendered pages before committing.
